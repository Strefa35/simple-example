# Build

    make
    or
    make -C /lib/modules/`uname -r`/build M=$PWD modules

# Clean

    make clean

# Loadable Module - list of commands

    insmod
    rmmod
    lsmod
    depmod
    modprobe
    modinfo
